import ExpCardInterface from "../types/ExpCardInterface";

const ExpCard = ({ company, position, employmentType,
    startDate, endDate, location, skills
}: ExpCardInterface) => {
    return <h2>ExpCard</h2>;
}

export default ExpCard;