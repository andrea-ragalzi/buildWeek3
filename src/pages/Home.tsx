import { Container, Row, Col } from "react-bootstrap";

const Home = () => {
  return (
    <Container className="pageContainer" fluid>
      <Row>

      </Row>
    </Container>
  );
};

export default Home;
